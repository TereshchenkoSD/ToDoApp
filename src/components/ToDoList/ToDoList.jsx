// import PropTypes from 'prop-types';
import ToDo from '../ToDo/ToDo';
// import todos from '';

const ToDoList = () => (
  <ul>
    {/* {items.map(item => (
      <li></li>
    ))} */}
  </ul>
);

export default ToDoList;
